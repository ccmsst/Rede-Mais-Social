// Habilidade.java (CORRIGIDA)
public class Habilidade { 
    private String nome;
    public Habilidade(String nome) { this.nome = nome; }
    
    // MÉTODO getNome() para uso no stream da Interface
    public String getNome() { return nome; }
}